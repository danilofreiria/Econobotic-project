const userExists = "Já existe usuário cadastrado com o e-mail informado.";

const missingInfo = "Todos os campos devem ser preenchidos!";

const invalidLogin = "Usuário e/ou senha inválido(s).";

const serverError = "Erro interno no servidor.";

const unauthorized = "Para acessar este recurso um token de autenticação válido deve ser enviado.";

const invalidEmail = "O e-mail informado já está sendo utilizado por outro usuário.";

const transactionNotFound = "Transação não encontrada.";

const invalidType = "O campo 'tipo' da transação é obrigatório e deve ser de ENTRADA ou SAIDA.";

const invalidID = "Id inválido.";

const invalidCategory = "A categoria especificada não existe.";
export {userExists, missingInfo, invalidLogin, serverError, 
    unauthorized, invalidEmail, transactionNotFound, invalidID,
    invalidType, invalidCategory};